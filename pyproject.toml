[project]
name = "data-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "dharsana2025", email = "dharsana263@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
data-project = "data_project:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
